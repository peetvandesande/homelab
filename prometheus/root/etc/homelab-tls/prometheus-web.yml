# TLS for the Prometheus web/API on :9090. Grafana's datasource and any browser
# reach it through this.
tls_server_config:
  cert_file: /etc/homelab-tls/host.crt
  key_file: /etc/homelab-tls/host.key
  min_version: TLS12
